# Темы 02 (NULL) и 04 (агрегация) пройдены; сгенерирована Тема 03 (CASE)

Со слов ученика (2026-09-11): прошёл **Тему 04 — Агрегация (GROUP BY / HAVING)** ([[0003-aggregation-group-by-having]]) — «в целом понятно». Одно слабое место: **путается, когда использовать `FILTER` в агрегате, а когда хватает `WHERE`** — запнулся на последней задаче (упр. 11, `COUNT(*) FILTER`). Также подтвердил, что **Тема 02 — NULL** ([[0004-null-and-three-valued-logic]]) прошла «без проблем».

**Обратная связь по методике (важно, повторять):** ученику зашло, что в урок вплетены упражнения с прошлых тем для повторения (interleaving / retrieval practice). Держать этот приём во всех будущих уроках.

## Что сделано
- Сгенерирован урок **Тема 03 — Выражения и CASE**: `lessons/0005-expressions-and-case.html` (12 упражнений).
  - §4 целиком про **`WHERE` vs `FILTER` vs `CASE`** — прямой ответ на его затык: `FILTER` = `CASE` внутри агрегата; правило выбора (одно условие на весь запрос → `WHERE`; несколько разных счётчиков в одной строке → `FILTER`).
  - Interleaving: упр. 5–6 повторяют NULL (`||` с NULL → COALESCE), упр. 7–12 повторяют агрегацию/JOIN.
  - Плюс `CASE` (searched/simple, ловушка «нет ELSE → NULL»), приведение типов `::` и **ловушка целочисленного деления** (backend-боль), строки/даты (`||`, `EXTRACT`).
- Все 12 эталонных решений **прогнаны на реальном Postgres** (временный локальный сервер + `seed.sql`) — проходят, числа сверены.
- Обновлены `index.html` (карточка Темы 03, метки 02/04 → «Пройдено») и `reference/syllabus.html` (Тема 03 разблокирована, «твой путь» переписан).

## Дальше по плану
- **Тема 05 — JOIN вглубь** (ядро, упор на беглость/скорость — см. [[0003-select-in-depth-fluent]] и NOTES). Ждём отчёт ученика по Теме 03 (сколько решил / где притормозил) для калибровки ZPD.
- Не забыть при следующем деплое: **запушить в репозиторий** (GitHub Pages — это «запуск» для ученика).
