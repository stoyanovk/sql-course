# NOTES — teaching preferences & working notes

## Learner (from claude-course-exersise/CLAUDE.md)
- Фулстек, 90% фронтенд: TypeScript, React, GraphQL, Apollo, SSR.
- Backend — учит: Node, Express, NestJS, **Postgres**, Redis, Python, aiohttp, SQLAlchemy.
- Хочет **короткие ответы, простые предложения**.
- Английский слабый → курс ведём **по-русски**; технические ключевые слова SQL оставляем как есть (SELECT, JOIN…).
- Для backend-кода любит объяснение решений («хочу учить backend»), не только ответ.

## How to teach here
- Одна идея на урок + сразу практика. Держись в рабочей памяти.
- Каждая тема ≥ 10 упражнений (запрос ученика).
- Практика — только в браузере (PGlite), с автопроверкой. Тесная петля.
- Каждый урок: короткое знание → эталонный источник (ссылка) → упражнения → «спроси учителя».
- Сложность в объяснениях — минимум; сложность в упражнениях — намеренная (retrieval, spacing, interleaving).
- Отвечать на вопросы ученика по SQL прямо в чате — я его учитель.
- **Fluency, не только correctness** (важно ученику, 2026-08-20): «решил» ≠ «решаю быстро и легко».
  Цель для JOIN — довести до автоматизма, как задачи 1–4 (беглость / storage strength), а не просто до правильного ответа.
  НЕ пропускать тему только потому, что задачи диагностики решены. Давать беглостную практику: много коротких JOIN,
  на скорость, с повтором через интервалы.

## ВАЖНО: кодировка
- Каждый HTML-файл ДОЛЖЕН начинаться с `<meta charset="utf-8">` (первой строкой, до <title>).
  Python http.server отдаёт `text/html` без charset → без meta браузер декодирует кириллицу как CP1251
  (mojibake вида «РљСѓСЂСЃ»). Meta-тег чинит это и на http, и на file://. Не забывать в новых уроках.

## Deployment / как ученик открывает курс
- ОСНОВНОЙ способ (по решению ученика 2026-08-20): курс задеплоен на **GitHub Pages** →
  https://stoyanovk.github.io/sql-course/ . Ему это проще, чем держать локальный дев-сервер.
  GitHub Pages отдаёт всё по https с правильным charset → ES-модули/WASM грузятся, mojibake нет.
- Следствие: `serve.py` / `serve.command` теперь **необязательны** (ученик сказал «дев-сервер можно дропнуть»).
  Файлы пока оставлены как локальный фолбэк для оффлайна/разработки; можно удалить по его слову.
- Значит: после правок уроков **пушить в репозиторий**, чтобы Pages обновились — это и есть «запуск» для ученика.

## Practice environment
- Движок: PGlite (настоящий Postgres в WASM), грузится с jsDelivr CDN → нужен интернет.
  Если CDN однажды сломает API — запинить версию в `assets/engine.js` (PGLITE_URL).
- Данные: маленький интернет-магазин, см. `reference/schema.html`. НЕ менять схему без обновления упражнений.
- (Локальный фолбэк, если нужен: `python3 serve.py`, порт 8000 — поднимает http.server с charset=utf-8.)

## Feedback channel
- Автопроверка даёт мгновенный ✓/✗, но НЕ видит, как далеко ученик дошёл.
  Проси его написать в чат: сколько задач решил / где застрял → на этом строю learning-records и следующий урок (ZPD).

## Open decisions / to revisit
- ✅ Диагностика пройдена (7/8, см. learning-record 0002). Калибровка: база SELECT прочна → повтор Темы 01 пропускаем,
  старт с Темы 04 (агрегация) — названное учеником слабое место («забываю GROUP BY»). Урок 0003 готов.
- ⚠️ JOIN НЕ пропускаем (по просьбе ученика 2026-08-20): задачи решены, но нужна беглость. План: после агрегации —
  отдельный урок «Тема 05 · JOIN вглубь» с упором на скорость/автоматизм. Плюс агрегация (0003) уже вплетает JOIN
  (упр. 9, 10, 12) — это уже беглостная практика JOIN. Глубину урока по JOIN откалибровать по тому, как пойдут эти упражнения.
- Задача 8 (оконные функции) — ученик тянулся к подзапросам. Дать оконки (Тема 08) позже; пока это тизер в чате.
- NULL/CASE (Темы 02–03) — не тестировались диагностикой в глубину; вплетать в практику, отдельный урок по мере надобности.
- ORM (SQLAlchemy) — за рамками, но это следующий очевидный мост к его backend-стеку. Вернуться, когда «голый» SQL закрепится.
